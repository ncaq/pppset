\documentclass[$if(fontsize)$$fontsize$,$endif$$if(lang)$$lang$,$endif$$if(papersize)$$papersize$$else$a4paper,$endif$$for(classoption)$$classoption$$sep$,$endfor$xcolor={svgnames},hyperref={unicode}]{$documentclass$}

\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{calc}
\usepackage{etoolbox}
\usepackage{fancyvrb}
\usepackage{float}
\usepackage{ifplatform}
\usepackage{ifthen}
\usepackage{indentfirst}
\usepackage{keyval}
\usepackage{kvoptions}
\usepackage{lineno}
\usepackage{listing}
\usepackage{longtable}
\usepackage{pdftexcmds}
\usepackage{url}

\usepackage{luatexja}
\usepackage{luatexja-fontspec}
\setmainfont{Ricty}
\setmonofont{Ricty}
\setsansfont{Ricty}
\setmainjfont{Ricty}
\setsansjfont{Ricty}

\setkeys{Gin}{width=\textwidth}

\def\figureautorefname{図}
\newcommand*{\fullref}[1]{\autoref{#1}: \nameref{#1}}

\usepackage{minted}
\usemintedstyle{emacs}
\renewcommand\theFancyVerbLine{\large\arabic{FancyVerbLine}}

\title{$title$}
\author{$author$}
\date{$date$}

$for(header-includes)$
$header-includes$
$endfor$

\begin{document}

$for(include-before)$
$include-before$
$endfor$

$body$

$for(include-after)$
$include-after$
$endfor$

\end{document}
