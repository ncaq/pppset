\documentclass[$fontsize$, $lang$, $papersize$, $for(classoption)$$classoption$$sep$, $endfor$xcolor={svgnames}, hyperref={unicode}]{$documentclass$}

\usepackage{luatexja}
\usepackage{luatexja-fontspec}
\setmainfont{Ricty}
\setmonofont{Ricty}
\setsansfont{Ricty}
\setmainjfont{Ricty}
\setsansjfont{Ricty}

\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{fancyvrb}
\usepackage{float}
\usepackage{indentfirst}
\usepackage{lineno}
\usepackage{listing}
\usepackage{longtable}
\usepackage{multicol}
\usepackage{sidecap}
\usepackage{ulem}
\usepackage{url}

\usepackage[hypcap=false]{caption}

\newcommand*{\fullref}[1]{\autoref{#1}: \nameref{#1}}
\AtBeginDocument{\renewcommand{\ref}[1]{\fullref{#1}}}

\usepackage{graphicx}
\setkeys{Gin}{width=\textwidth, height=\textheight, keepaspectratio}
\def\figureautorefname{図}

\usepackage{minted}
\usemintedstyle{emacs}
\newcommand{\mintlang}{text}
\renewcommand\theFancyVerbLine{\arabic{FancyVerbLine}}

\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\title{$title$}
\author{$author$}
\date{$date$}

$for(header-includes)$
$header-includes$
$endfor$

\begin{document}

$for(include-before)$
$include-before$
$endfor$

$body$

$for(include-after)$
$include-after$
$endfor$

\end{document}
